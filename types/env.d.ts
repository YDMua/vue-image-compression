// 此文件由 edon-service 自动生成，请勿手动修改
declare global {
  const env: {
    NODE_ENV: string;
    PORT: string;
    MODE: string;
  }

  namespace NodeJS {
    interface ProcessEnv {
    NODE_ENV: string;
    PORT: string;
    MODE: string;
    }
  }
}

export {};
